{% extends 'base.html.twig' %}

{% block title %}ArtistEntity{% endblock %}

{% block body %}
    <h1>ArtistEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artist_entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_web_artist_index') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_web_artist_edit', {'id': artist_entity.id}) }}">edit</a>
        {{ include('web/artist/_delete_form.html.twig') }}

    {% endif %}
{% endblock %}
