{% extends 'base.html.twig' %}

{% block title %}TrackEntity{% endblock %}

{% block body %}
    <h1>TrackEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ track_entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ track_entity.title }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ track_entity.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_web_controller_track_index') }}">back to list</a>

    <a href="{{ path('app_web_controller_track_edit', {'id': track_entity.id}) }}">edit</a>

    {{ include('web/controller/track/_delete_form.html.twig') }}
{% endblock %}
